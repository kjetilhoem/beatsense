package com.beatsense.audio

/**
 * Shared audio pipeline configuration.
 * Framework-free — usable from both production code and tests.
 */
object AudioConfig {
    const val SAMPLE_RATE = 44100
    const val BUFFER_SIZE = 4096
}
